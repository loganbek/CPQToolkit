CPQ DEVELOPER TOOLKIT

The CPQ Developer Toolkit requires Java 11 or later installed on your computer.
If you do not have a suitable Java Runtime Environment installation, go to https://www.oracle.com/technetwork/java/javase/downloads/index.html to download it.

To run the CPQ Developer Toolkit from the command line, execute the cpq-toolkit script.
For example, if your current directory is the directory that contains this README file, you can run the following command:
    ./bin/cpq-toolkit --help
